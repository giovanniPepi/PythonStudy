# If you specify a default value for a paramter, no spaces should be used 
# on either side of the equal sign:

def function_name(parameter_0, parameter_1='default value')

# the same is valid for keyword arguments in function calls:

function_name(value_0, parameter_1='value')